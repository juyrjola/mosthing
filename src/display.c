#include <mgos.h>
//#include <mgos_arduino_ssd1306.h>

void display_init(void)
{
#if 0
    Adafruit_SSD1306 *display;

    display = mgos_ssd1306_create_i2c(-1, MGOS_SSD1306_RES_128_64);
    if (display == NULL) {
        LOG(LL_ERROR, "Unable to create ")
    }
#endif
}
